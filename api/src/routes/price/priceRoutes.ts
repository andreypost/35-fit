import { NextFunction, Request, Response, Router } from "express";


export const price = Router()

// price.get('check-set', async (req: Request, res: Response, next: NextFunction): Promise<string> => {
//     try {
//         return 's'
//     } catch (error: any) {
//         next(error)
//     }
// })