{
  "phases": {
    "install": {
      "cmds": ["cd ./api && npm install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["cd ./api && npm run build"]
    },
    "start": {
      "cmd": "echo 'Starting application' && cd ./api && npm start"
    }
  }
}
