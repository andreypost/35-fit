{
  "phases": {
    "install": {
      "cmds": ["cd ./api && npm install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["cd ./api && npm run build"]
    }
  },
  "start": "cd ./api && npm start"
}
